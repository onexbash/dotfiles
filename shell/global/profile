# -- -- -- -- -- -- -- -- -- -- -- #
# --    GLOBAL SHELL PROFILE    -- #
# -- -- -- -- -- -- -- -- -- -- -- #
# --        /etc/profile        -- #
# -- -- -- -- -- -- -- -- -- -- -- #

# System-wide profile for sh(1)
if [ "${BASH-no}" != "no" ]; then
	[ -r /etc/bashrc ] && . /etc/bashrc
fi
